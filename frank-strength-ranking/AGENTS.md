# Expo HAS CHANGED

This project is pinned to Expo SDK 54 (`expo: "54"` in package.json). Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code — APIs like `expo-symbols` and `expo-router`'s native tabs change shape between SDK versions.
